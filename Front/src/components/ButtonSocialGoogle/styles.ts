import {StyleSheet} from 'react-native';
import { RFValue } from 'react-native-responsive-fontsize';

export const styles = StyleSheet.create({
    Button:{
    marginTop: 10,
    marginLeft:30,
    borderRadius:20,
    width: 25,
    height: RFValue(25),
    backgroundColor: "#8AC449",
    alignItems: "center",
    }});
